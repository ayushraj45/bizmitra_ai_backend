// import express from 'express';
// import { authenticateToken } from '../middleware/auth.js'; // Adjust path as needed
// import whatsappController from '../controllers/whatsappController.js';

// // const router = express.Router();

// // router.post('/embedded-signup-callback', authenticateToken, whatsappController.handleEmbeddedSignupCallback);
// // router.get('/signup-status', authenticateToken, whatsappController.getSignupStatus);
// // router.post('/get-signup-url', authenticateToken, whatsappController.getSignupUrl);

// export default router;